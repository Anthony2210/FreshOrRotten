# Modèle TensorFlow.js

Ce dossier recevra le modèle converti pour l'interface web.

Fichiers attendus après conversion :

- `model.json` ;
- un ou plusieurs fichiers `.bin`.

Ces fichiers viennent du modèle Keras entraîné en Python.
Ils ne doivent pas être inventés ni remplacés par un faux modèle.
